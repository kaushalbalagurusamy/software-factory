# Coverage: TB-10 sub-agent stop validators

Cases: `evals/agents/test_tb10_subagent_stop.py`. Golden 10, diagnostic 43. Requirement: R-16. Audit and design fixtures are git repositories whose report files are new and untracked, so every row and file is "new" or "changed" under any reading (C-11, C-13).

| PRD behaviour | Cases |
|---|---|
| 1. Validator chosen by agent_type; implement, test, orchestrator and unknown types allowed | G-010 (implement), D-039 to D-041 (test, orchestrator, unknown); research/audit/review/design routing exercised by every other case |
| 2. research: Sources heading or bold label followed by a URL or path; `unverified` sentences marked; at most 8,000 characters | G-001, G-002, D-001 (heading + path), D-002 (bold + URL), D-003 (no reference), D-004 (URL only before the heading), D-005 (plain word), D-006 (unmarked unverified), D-007 / D-008 (8,000 / 8,001), D-009 (empty), D-010 (message absent, command line) |
| 3. audit: new rows in `<audit_dir>/BUGS-MITIGATIONS.md` need a severity and a `<path>:<line>` citation; missing file only with `no findings` | G-003, G-004, D-012 (case-insensitive), D-013 (all four severities), D-014 (unknown severity), D-015 (no citation), D-016 (path without line), D-017 / D-018 (missing file with and without `no findings`), D-019 / D-020 (alt `notes/audit`) |
| 4. review: VERDICT line; REJECT needs a citation; grade lines; `no passing cases`; wrong_reason with APPROVE blocks | G-005, G-006, G-007, D-022 (no verdict), D-023 (valid REJECT), D-024 (no grades), D-025 (`no passing cases`), D-026 (wrong_reason with REJECT allowed), D-027 (REJECT without grades), D-029 (inconclusive counts as a grade line) |
| 5. design: spec-named files under design_dirs need four headings; every Axioms item has `check:`; one-way door needs an ADR | G-008, G-009, D-030 (two headings missing), D-031 (Non-goals does not satisfy Goals), D-032 (axiom without check), D-033 / D-034 (one-way door without and with ADR), D-035 (non-spec name), D-036 (outside design dirs), D-037 / D-038 (alt design dirs) |
| 6. Each block reason lists every missing element | G-008 (Axioms), D-011 (Sources + unverified), D-021 (severity + citation), D-028 (verdict + grade), D-030 (Non-goals + Open questions) |
| 7. Reads only the message and files under the project root; no network | D-042 (sockets patched to fail) |
| hook-io adapter for SubagentStop | D-010, D-043 |

## Not tested and why
- Behaviour 7 "reads only files under the project root" has no observable seam beyond the no-network check.
- Rows that existed before the run (C-11); conventional tables with header and separator lines (C-12); design files that were committed and not changed (C-13).
- ADR location under a non-default profile (`design/adr` in alt) (C-14).
- `inconclusive` grade with `VERDICT: APPROVE` (C-15); casing variants of `unverified` and `**Sources:**` (C-16); a message with both verdicts (C-17).
- Reason wording: cases check element names case-insensitively only (C-18).
