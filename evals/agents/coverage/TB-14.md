# Coverage: TB-14 end-to-end walking skeleton and shadow routing log

Cases: `evals/agents/test_tb14_skeleton.py`. Golden 9, diagnostic 46. Requirements: R-20, R-19, R-01 (some also R-09, R-15, R-24). Sources: the PRD, all contracts, and `docs/contracts/clarifications.md` (binding). The stream is decided by `run_events(project_root, [{"payload": ...}])` returning `Outcome(kind, reasons)` (C-26); the fixture profile is base without `mcp_servers` (C-29).

| PRD behaviour | Cases |
|---|---|
| 1. run_events loads the profile and combines outcomes (deny/block over ask over allow) | D-040 (one Outcome per event, in order), D-041 (allow has no reasons), D-042 (deny carries reasons), D-043 (deny beats ask, both reasons), every stream case |
| 2. Agents and settings from the profile; the scripted stream | G-001 to G-008, D-017, D-018, D-019, D-020, D-021, D-044 / D-045 (implement stop through the SubagentStop registration of `stop_gate`, C-28) |
| 3. Only the profile changes outcomes | D-001 to D-015, D-016 |
| 4. shadow.record(result, actual_choice, ledger_dir, state_digest="") | G-009 (line, `agrees`, `state_digest`), D-025 to D-034, D-035 (log never steers decisions), D-037 (`agrees` = `result.choice == actual_choice`, C-27), D-038 (missing `ledger_dir` created, C-27), D-039 (`state_digest` default `""`), D-046 (duck-typed result with only the attributes `log_record` reads, C-33) |
| 5. No network, no model call | D-022 (every hook command line with sockets disabled), D-023 (`run_events` in process), D-024 (shadow) |
| 6. Runbook | D-036 |

## Not tested and why
- The frontmatter Stop registration running as SubagentStop is still *unverified* (C-34). It belongs to the manual live rehearsal, and no case depends on it.
