# Coverage matrix: TB-06 implement blindness guard

Cases: `evals/agents/test_tb06_blindness_guard.py`. Golden: TB06-G-001 to G-010 (10). Diagnostic: TB06-D-001 to D-181 (181). The fixture holds eval and held-out files with a content marker, a directory symlink `src/alias -> evals` and a file symlink `peek.py -> evals/case_a.py`. Both profiles are used (alt protects `qa/cases`, `qa/golden`, `qa/secret` and not `evals`).

| PRD behaviour | Cases |
|---|---|
| 1. Applies only to `implement`; every other role gets allow | G-010, D-161 to D-169; main session mapped to implement: D-170 |
| 2. Protected = `paths.eval` + `paths.held_out`, after normalisation (cwd, `.`/`..`, symlinks) | G-001, G-004, D-001 to D-017, D-026, D-031, D-077, D-078, D-079 |
| 3. Read: protected `file_path`; Grep/Glob: protected path or glob, and a missing path, `.`, or a strict ancestor | G-001, G-002, G-003, D-018 to D-039, D-179 |
| 4a. Bash naming a protected path (relative, absolute, parent, quoted, glob, tilde, `$VAR`) | G-005, D-040 to D-083 |
| 4b. Recursive search or listing on `.`, no path, or a strict ancestor | G-006, D-084 to D-104 |
| 4c. pytest, python -m pytest, unittest, node --test on a protected path | G-007, D-105 to D-110 |
| 4d. Interpreter one-liners naming a protected path or calling a directory walker | G-008, D-111 to D-123 |
| 4e. Dynamic construction: eval, base64 -d, xxd -r, sh/bash -c with command substitution, pipe into sh/bash | G-009, D-124 to D-131 |
| 5. Other Bash allowed: builds, own tests, git diff of own files, ls src, other dirs | D-132 to D-160, D-180 |
| 6. Reason says the path is protected without echoing contents; the hook never opens a protected file | D-171 to D-177; command line: D-178 |
| 7. Residual gap documented in `docs/hooks/blindness_guard.md` (OS sandbox as backstop) | D-181 (also R-24) |

Not tested: the points in `ambiguities-B.md` A-06-1 to A-06-7.
